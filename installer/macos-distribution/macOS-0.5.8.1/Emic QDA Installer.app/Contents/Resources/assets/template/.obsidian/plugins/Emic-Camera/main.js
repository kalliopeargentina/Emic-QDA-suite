/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __markAsModule = (target) => __defProp(target, "__esModule", { value: true });
var __export = (target, all) => {
  __markAsModule(target);
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __reExport = (target, module2, desc) => {
  if (module2 && typeof module2 === "object" || typeof module2 === "function") {
    for (let key of __getOwnPropNames(module2))
      if (!__hasOwnProp.call(target, key) && key !== "default")
        __defProp(target, key, { get: () => module2[key], enumerable: !(desc = __getOwnPropDesc(module2, key)) || desc.enumerable });
  }
  return target;
};
var __toModule = (module2) => {
  return __reExport(__markAsModule(__defProp(module2 != null ? __create(__getProtoOf(module2)) : {}, "default", module2 && module2.__esModule && "default" in module2 ? { get: () => module2.default, enumerable: true } : { value: module2, enumerable: true })), module2);
};
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e) {
        reject(e);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};

// main.ts
__export(exports, {
  default: () => main_default
});

// src/App.ts
var import_obsidian3 = __toModule(require("obsidian"));

// src/Modal.ts
var import_obsidian = __toModule(require("obsidian"));

// src/locales/en.ts
var en = {
  modal: {
    loading: "Loading..",
    startRecording: "Start recording",
    stopRecording: "Stop Recording",
    switchCamera: "Switch Camera",
    takeSnap: "Take a snap",
    chooseVideoFile: "Choose video file",
    uploadVideo: "\u21BF Upload video",
    chooseImageFile: "Choose image file",
    uploadImage: "\u21BF Upload image",
    chooseImageOrVideoFile: "Choose image or video file",
    upload: "\u21BF Upload",
    useButtonsBelow: "Use the buttons below to add video or images.",
    errorLoadingStream: "Error in loading videostream in your device..",
    addingNewImage: "Adding new Image to vault...",
    addingNewVideo: "Adding new Video to vault...",
    savedTo: "Saved to {{path}}",
    videoLabel: "Video: {{name}}",
    imageLabel: "Image: {{name}}",
    selectedLabel: "Selected: {{name}}"
  },
  settings: {
    title: "Emic-Camera settings",
    folderPath: "Folder Path",
    folderPathDesc: "Folder where the videos and snaps should be saved",
    folderPathPlaceholder: "e.g. attachments/snaps",
    maxEmbedWidth: "Max embed width (px)",
    maxEmbedWidthDesc: "Maximum width in pixels for embedded images and videos (0 = no limit)",
    maxEmbedHeight: "Max embed height (px)",
    maxEmbedHeightDesc: "Maximum height in pixels for embedded images and videos (0 = no limit)",
    placeholderWidth: "800",
    placeholderHeight: "600"
  },
  app: {
    ribbonTitle: "Emic-Camera",
    commandName: "Open Emic-Camera"
  }
};

// src/locales/es.ts
var es = {
  modal: {
    loading: "Cargando...",
    startRecording: "Iniciar grabaci\xF3n",
    stopRecording: "Detener grabaci\xF3n",
    switchCamera: "Cambiar c\xE1mara",
    takeSnap: "Tomar foto",
    chooseVideoFile: "Elegir archivo de video",
    uploadVideo: "\u21BF Subir video",
    chooseImageFile: "Elegir archivo de imagen",
    uploadImage: "\u21BF Subir imagen",
    chooseImageOrVideoFile: "Elegir imagen o video",
    upload: "\u21BF Subir",
    useButtonsBelow: "Usa los botones de abajo para a\xF1adir video o im\xE1genes.",
    errorLoadingStream: "Error al cargar la transmisi\xF3n de video en tu dispositivo.",
    addingNewImage: "A\xF1adiendo imagen al vault...",
    addingNewVideo: "A\xF1adiendo video al vault...",
    savedTo: "Guardado en {{path}}",
    videoLabel: "Video: {{name}}",
    imageLabel: "Imagen: {{name}}",
    selectedLabel: "Seleccionado: {{name}}"
  },
  settings: {
    title: "Ajustes de Emic-Camera",
    folderPath: "Carpeta",
    folderPathDesc: "Carpeta donde se guardar\xE1n los videos y las fotos",
    folderPathPlaceholder: "ej. attachments/snaps",
    maxEmbedWidth: "Ancho m\xE1ximo del embed (px)",
    maxEmbedWidthDesc: "Ancho m\xE1ximo en p\xEDxeles para im\xE1genes y videos embebidos (0 = sin l\xEDmite)",
    maxEmbedHeight: "Alto m\xE1ximo del embed (px)",
    maxEmbedHeightDesc: "Alto m\xE1ximo en p\xEDxeles para im\xE1genes y videos embebidos (0 = sin l\xEDmite)",
    placeholderWidth: "800",
    placeholderHeight: "600"
  },
  app: {
    ribbonTitle: "Emic-Camera",
    commandName: "Abrir Emic-Camera"
  }
};

// src/i18n.ts
var locales = {
  en,
  es
};
var defaultLocale = "en";
function getLocale() {
  const lang = typeof navigator !== "undefined" ? navigator.language : defaultLocale;
  const base = lang.split("-")[0].toLowerCase();
  return locales[base] ? base : defaultLocale;
}
var currentLocale = getLocale();
function getNested(obj, path) {
  const parts = path.split(".");
  let cur = obj;
  for (const p of parts) {
    cur = cur == null ? void 0 : cur[p];
    if (cur === void 0)
      return void 0;
  }
  return typeof cur === "string" ? cur : void 0;
}
function t(key, params) {
  var _a, _b, _c;
  const dict = (_a = locales[currentLocale]) != null ? _a : locales[defaultLocale];
  let value = (_c = (_b = getNested(dict, key)) != null ? _b : getNested(en, key)) != null ? _c : key;
  if (params) {
    for (const [k, v] of Object.entries(params)) {
      value = value.replace(`{{${k}}}`, v);
    }
  }
  return value;
}

// src/Modal.ts
var CameraModal = class extends import_obsidian.Modal {
  constructor(app2, cameraSettings) {
    super(app2);
    this.videoStream = null;
    this.cameraSettings = cameraSettings;
    this.chosenFolderPath = cameraSettings.chosenFolderPath;
  }
  onOpen() {
    return __async(this, null, function* () {
      const { contentEl } = this;
      const webCamContainer = contentEl.createDiv();
      const isAndroid = /android/i.test(navigator.userAgent);
      const isMobile = /android|iphone|ipad|ipod|mobile/i.test(navigator.userAgent);
      const statusMsg = webCamContainer.createEl("span", {
        text: t("modal.loading")
      });
      const videoEl = webCamContainer.createEl("video");
      const buttonsDiv = webCamContainer.createDiv();
      const firstRow = buttonsDiv.createDiv();
      const secondRow = buttonsDiv.createDiv();
      const recordVideoButton = firstRow.createEl("button", {
        text: t("modal.startRecording")
      });
      const switchCameraButton = firstRow.createEl("button", {
        text: t("modal.switchCamera")
      });
      const snapPhotoButton = firstRow.createEl("button", {
        text: t("modal.takeSnap")
      });
      firstRow.style.display = "none";
      secondRow.style.display = "none";
      if (isMobile) {
        const filePickerVideo = secondRow.createEl("input", {
          placeholder: t("modal.chooseVideoFile"),
          type: "file"
        });
        filePickerVideo.id = "filepicker-video";
        filePickerVideo.accept = "video/*";
        filePickerVideo.capture = "camcorder";
        filePickerVideo.style.display = "none";
        const labelVideo = secondRow.createEl("label");
        labelVideo.addClass("emic-camera-upload-btn");
        labelVideo.htmlFor = "filepicker-video";
        labelVideo.innerHTML = t("modal.uploadVideo");
        labelVideo.appendChild(filePickerVideo);
        secondRow.appendChild(labelVideo);
        const filePickerImage = secondRow.createEl("input", {
          placeholder: t("modal.chooseImageFile"),
          type: "file"
        });
        filePickerImage.id = "filepicker-image";
        filePickerImage.accept = "image/*";
        filePickerImage.capture = "camera";
        filePickerImage.style.display = "none";
        const labelImage = secondRow.createEl("label");
        labelImage.addClass("emic-camera-upload-btn");
        labelImage.htmlFor = "filepicker-image";
        labelImage.innerHTML = t("modal.uploadImage");
        labelImage.appendChild(filePickerImage);
        secondRow.appendChild(labelImage);
      } else {
        const filePicker = secondRow.createEl("input", {
          placeholder: t("modal.chooseImageOrVideoFile"),
          type: "file"
        });
        filePicker.id = "filepicker";
        filePicker.accept = "image/*,video/*";
        filePicker.style.display = "none";
        const labelUpload = secondRow.createEl("label");
        labelUpload.addClass("emic-camera-upload-btn");
        labelUpload.htmlFor = "filepicker";
        labelUpload.innerHTML = t("modal.upload");
        labelUpload.appendChild(filePicker);
        secondRow.appendChild(labelUpload);
      }
      videoEl.autoplay = true;
      videoEl.muted = true;
      const chunks = [];
      let recorder = null;
      this.videoStream = null;
      const view = this.app.workspace.getActiveViewOfType(import_obsidian.MarkdownView);
      const saveFile = (file, isImage = false, fileName = "") => __async(this, null, function* () {
        if (!fileName) {
          const dateString = (new Date() + "").slice(4, 28).split(" ").join("_").split(":").join("-");
          fileName = isImage ? `image_${dateString}.png` : `video_${dateString}.webm`;
        }
        new import_obsidian.Notice(isImage ? t("modal.addingNewImage") : t("modal.addingNewVideo"));
        const filePath = this.chosenFolderPath + "/" + fileName;
        const folderExists = app.vault.getAbstractFileByPath(this.chosenFolderPath);
        if (!folderExists)
          yield app.vault.createFolder(this.chosenFolderPath);
        const fileExists = app.vault.getAbstractFileByPath(filePath);
        if (!fileExists)
          yield app.vault.createBinary(filePath, file);
        if (!view)
          return new import_obsidian.Notice(t("modal.savedTo", { path: filePath }));
        const cursor = view.editor.getCursor();
        const w = this.cameraSettings.maxEmbedWidth || 0;
        const h = this.cameraSettings.maxEmbedHeight || 0;
        const sizePart = w > 0 && h > 0 ? `|${w}x${h}` : w > 0 ? `|${w}` : "";
        const embedStr = isImage ? `![[${filePath}${sizePart}]]
` : `
![[${filePath}${sizePart}]]
`;
        view.editor.replaceRange(embedStr, cursor);
        this.close();
      });
      const handleImageSelectChange = (file, isImage = true) => __async(this, null, function* () {
        const bufferFile = yield file.arrayBuffer();
        saveFile(bufferFile, isImage, file.name.split(" ").join("-"));
      });
      if (isMobile) {
        const filePickerVideo = secondRow.querySelector("#filepicker-video");
        const labelVideo = secondRow.querySelector("label[for='filepicker-video']");
        const filePickerImage = secondRow.querySelector("#filepicker-image");
        const labelImage = secondRow.querySelector("label[for='filepicker-image']");
        filePickerVideo.onchange = () => {
          var _a;
          if (!((_a = filePickerVideo.files) == null ? void 0 : _a.length))
            return;
          const selectedFile = filePickerVideo.files[0];
          labelVideo == null ? void 0 : labelVideo.setText(t("modal.videoLabel", { name: selectedFile.name }));
          handleImageSelectChange(selectedFile, false);
        };
        filePickerImage.onchange = () => {
          var _a;
          if (!((_a = filePickerImage.files) == null ? void 0 : _a.length))
            return;
          const selectedFile = filePickerImage.files[0];
          labelImage == null ? void 0 : labelImage.setText(t("modal.imageLabel", { name: selectedFile.name }));
          handleImageSelectChange(selectedFile, true);
        };
      } else {
        const filePicker = secondRow.querySelector("#filepicker");
        const labelUpload = secondRow.querySelector("label[for='filepicker']");
        filePicker.onchange = () => {
          var _a;
          if (!((_a = filePicker.files) == null ? void 0 : _a.length))
            return;
          const selectedFile = filePicker.files[0];
          const isImage = selectedFile.type.startsWith("image/");
          labelUpload == null ? void 0 : labelUpload.setText(t("modal.selectedLabel", { name: selectedFile.name }));
          handleImageSelectChange(selectedFile, isImage);
        };
      }
      if (isAndroid) {
        videoEl.style.display = "none";
        statusMsg.textContent = t("modal.useButtonsBelow");
        secondRow.style.display = "block";
      } else {
        const cameras = (yield navigator.mediaDevices.enumerateDevices()).filter((d) => d.kind === "videoinput");
        if (cameras.length <= 1)
          switchCameraButton.style.display = "none";
        let cameraIndex = 0;
        const getVideoStream = () => __async(this, null, function* () {
          try {
            return yield navigator.mediaDevices.getUserMedia({
              video: { deviceId: cameras[cameraIndex].deviceId },
              audio: true
            });
          } catch (error) {
            console.log(error);
            return null;
          }
        });
        this.videoStream = yield getVideoStream();
        if (this.videoStream) {
          firstRow.style.display = "block";
          secondRow.style.display = "block";
          statusMsg.style.display = "none";
        } else {
          secondRow.style.display = "block";
          statusMsg.textContent = t("modal.errorLoadingStream");
        }
        switchCameraButton.onclick = () => __async(this, null, function* () {
          cameraIndex = (cameraIndex + 1) % cameras.length;
          this.videoStream = yield navigator.mediaDevices.getUserMedia({
            video: { deviceId: cameras[cameraIndex].deviceId },
            audio: true
          });
          videoEl.srcObject = this.videoStream;
          videoEl.play();
        });
        snapPhotoButton.onclick = () => {
          const canvas = webCamContainer.createEl("canvas");
          canvas.style.display = "none";
          const { videoHeight, videoWidth } = videoEl;
          canvas.height = videoHeight;
          canvas.width = videoWidth;
          canvas.getContext("2d").drawImage(videoEl, 0, 0, videoWidth, videoHeight);
          canvas.toBlob((blob) => __async(this, null, function* () {
            const bufferFile = yield blob.arrayBuffer();
            saveFile(bufferFile, true);
          }), "image/png");
        };
        if (this.videoStream)
          videoEl.srcObject = this.videoStream;
        recordVideoButton.onclick = () => __async(this, null, function* () {
          switchCameraButton.disabled = true;
          if (!recorder) {
            recorder = new MediaRecorder(this.videoStream, {
              mimeType: "video/webm"
            });
          }
          let isRecording = recorder && recorder.state === "recording";
          if (isRecording) {
            recorder.stop();
          } else {
            recorder.start();
          }
          isRecording = !isRecording;
          recordVideoButton.innerText = isRecording ? t("modal.stopRecording") : t("modal.startRecording");
          recorder.ondataavailable = (e) => chunks.push(e.data);
          recorder.onstop = (_) => __async(this, null, function* () {
            const blob = new Blob(chunks, {
              type: "audio/ogg; codecs=opus"
            });
            const bufferFile = yield blob.arrayBuffer();
            saveFile(bufferFile, false);
          });
        });
      }
    });
  }
  onClose() {
    var _a;
    const { contentEl } = this;
    (_a = this.videoStream) == null ? void 0 : _a.getTracks().forEach((track) => {
      track.stop();
    });
    contentEl.empty();
  }
};
var Modal_default = CameraModal;

// src/SettingsTab.ts
var import_obsidian2 = __toModule(require("obsidian"));
var DEFAULT_SETTINGS = {
  chosenFolderPath: "attachments/snaps",
  maxEmbedWidth: 800,
  maxEmbedHeight: 600
};
var CameraSettingsTab = class extends import_obsidian2.PluginSettingTab {
  constructor(app2, plugin) {
    super(app2, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.createEl("h2", { text: t("settings.title") });
    new import_obsidian2.Setting(containerEl).setName(t("settings.folderPath")).setDesc(t("settings.folderPathDesc")).addText((text) => text.setPlaceholder(t("settings.folderPathPlaceholder")).setValue(this.plugin.settings.chosenFolderPath).onChange((value) => __async(this, null, function* () {
      this.plugin.settings.chosenFolderPath = value;
      yield this.plugin.saveSettings();
    })));
    new import_obsidian2.Setting(containerEl).setName(t("settings.maxEmbedWidth")).setDesc(t("settings.maxEmbedWidthDesc")).addText((text) => text.setPlaceholder(t("settings.placeholderWidth")).setValue(String(this.plugin.settings.maxEmbedWidth || "")).onChange((value) => __async(this, null, function* () {
      const n = parseInt(value, 10);
      this.plugin.settings.maxEmbedWidth = isNaN(n) || n < 0 ? 0 : n;
      yield this.plugin.saveSettings();
    })));
    new import_obsidian2.Setting(containerEl).setName(t("settings.maxEmbedHeight")).setDesc(t("settings.maxEmbedHeightDesc")).addText((text) => text.setPlaceholder(t("settings.placeholderHeight")).setValue(String(this.plugin.settings.maxEmbedHeight || "")).onChange((value) => __async(this, null, function* () {
      const n = parseInt(value, 10);
      this.plugin.settings.maxEmbedHeight = isNaN(n) || n < 0 ? 0 : n;
      yield this.plugin.saveSettings();
    })));
  }
};

// src/App.ts
var ObsidianCamera = class extends import_obsidian3.Plugin {
  onload() {
    return __async(this, null, function* () {
      yield this.loadSettings();
      this.addRibbonIcon("camera", t("app.ribbonTitle"), (evt) => {
        new Modal_default(this.app, this.settings).open();
      });
      this.addSettingTab(new CameraSettingsTab(this.app, this));
      this.addCommand({
        id: "Open camera modal",
        name: t("app.commandName"),
        callback: () => {
          new Modal_default(this.app, this.settings).open();
        }
      });
    });
  }
  loadSettings() {
    return __async(this, null, function* () {
      this.settings = Object.assign({}, DEFAULT_SETTINGS, yield this.loadData());
    });
  }
  saveSettings() {
    return __async(this, null, function* () {
      yield this.saveData(this.settings);
    });
  }
};

// main.ts
var main_default = ObsidianCamera;
